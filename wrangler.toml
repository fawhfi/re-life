name = "relife-app"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

[build]
command = "npm install && npm run build"
cwd = "frontend"

[build.environment]
NODE_VERSION = "18"

# 环境变量在 Cloudflare Pages Dashboard 中配置
# 或使用 wrangler pages secret 命令上传
